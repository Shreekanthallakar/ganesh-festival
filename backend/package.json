{
  "name": "ganesh-festival-gallery-backend",
  "version": "1.0.0",
  "description": "Admin-key protected upload/delete backend for the Ganesh Festival photo gallery, storing images directly in the user's own GitHub repo.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
